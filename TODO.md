# TODO

Живой список задач по репозиторию. Обновляется после каждого automation round.

## Выполнено в этом раунде (2026-04-10, automation round #3)

- [x] Архивирован legacy-тайваньский корпус (1,734 файла) в `Pilot_Reports_LEGACY/` для уменьшения шума в автогенерируемых артефактах.
- [x] Очистка пустых директорий в `Pilot_Reports/` после миграции тайваньских отчётов.
- [x] Пересобраны `themes/` (30 тематических страниц), `WIKILINKS.md` (154 уникальных викалинков) и `network/`; все скрипты проходят проверку `.venv/bin/python -m py_compile`.
- [x] Локальные RU-отчёты: 43 файла (MOEXBC + MOEXBMI полное покрытие).
- [x] На 2026-04-10 `gh issue list` и `gh pr list` по-прежнему недоступны без `gh auth login` (HTTP 401).

## Следующие действия

- [ ] Дожать источник финансовых данных для `X5`: карточка уже заведена по официальному составу `MOEXBC`, но текущий `yfinance` не отдаёт пригодные метаданные, valuation snapshot и финансовые таблицы.
- [ ] Дожать источник финансовых данных для `YDEX` и `OZON`: текущий `yfinance` по-прежнему не отдаёт пригодные метаданные и финансовые таблицы даже после явных alias-кандидатов.
- [ ] Найти источник полных финансовых таблиц для `T`: `TCSG.ME` уже даёт капитализацию, EV и valuation snapshot, но годовые и квартальные формы в `yfinance` остаются пустыми.
- [ ] Проверить очередь `MOEXBC` на актуальность состава и покрытие.
- [ ] Рассмотреть добавление русскоязычных описаний для оставшихся MOEX-бумаг (MOEXBC: 15/15, MOEXBMI: 20/20).

## Внешние очереди

- [ ] Открытые issues: на 2026-04-10 `gh issue list --repo denis-gordeev/My-RU-Coverage` недоступен без авторизации и возвращает `HTTP 401: Requires authentication`; нужна авторизация через `gh auth login` или публичная ручная перепроверка.
- [ ] Открытые PR: на 2026-04-10 `gh pr list --repo denis-gordeev/My-RU-Coverage --json number,title,headRefName,state` недоступен без авторизации и возвращает `HTTP 401: Requires authentication`; нужна авторизация через `gh auth login` или публичная ручная перепроверка.
